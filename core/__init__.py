"""Lan-Share core shared library.

Modules:
    protocol : TCP framing / message helpers
    capture  : screen capture abstraction
    player   : stream viewer (tkinter based with headless fallback)
"""

__version__ = "1.0.0"
